# @package _global_

log_aux_metrics: True
train_dataset:
  use_splash: False

model:
  intermediates_to_log:
    mlp: [act, preact, out]
    attn: [logits, out]
    emb: [emb]
    unemb: [logits]
    blocks: [out_attn_block, out_mlp_block]
