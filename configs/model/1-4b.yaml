# @package _global_

# about 1.4B params with DCLM tokenizer embeddings (& biases)
model:
  d_model: 1792
  num_heads: 14
  d_head: 128
  num_layers: 24
  mlp_factor: 2.7142858

  use_gating_mlp: True
