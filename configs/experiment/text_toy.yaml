# @package _global_

defaults:
  - /dataset/staircase@training_dataset
  # - /dataset/staircase@eval_list.0.dataset

eval_list:
  - dataset:
      name: NUMBER_STAIRCASE
      path: ""
      split: TEST
      seq_len: 1
      global_batch_size: 16
    evaluator: AUTOREGRESSIVE_ROLLOUTS

model:
  transformer_type: DISCRETE
  is_causal: True
  num_vocab: 10
