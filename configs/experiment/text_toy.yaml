# @package _global_

defaults:
  - /dataset/staircase@experiment.training_dataset
  - /dataset/staircase@experiment.eval_list.0.dataset

experiment:
  eval_list:
    - dataset:
        split: TEST
        seq_len: 1
        global_batch_size: 16
      evaluator: AUTOREGRESSIVE_ROLLOUTS

model:
  transformer_type: DISCRETE
  is_causal: True
  num_vocab: 10
